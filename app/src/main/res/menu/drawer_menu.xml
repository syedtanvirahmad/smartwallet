<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:title="Expense">
        <menu>
            <item
                android:id="@+id/nav_addIncomeSource"
                android:icon="@drawable/money_icon"
                android:title="@string/add_income_source" />
            <item
                android:id="@+id/nav_addIncome"
                android:icon="@drawable/money_icon"
                android:title="@string/add_income" />
            <item
                android:id="@+id/nav_viewIncome"
                android:icon="@drawable/money_icon"
                android:title="@string/view_income" />
            <item
                android:id="@+id/nav_viewExpenses"
                android:icon="@drawable/money_icon"
                android:title="@string/view_expense" />
        </menu>
    </item>

    <item android:title="Dashboard">
        <menu>
            <item
                android:id="@+id/dashboard"
                android:icon="@mipmap/ic_launcher"
                android:title="View Dashboard" />
        </menu>
    </item>
    <item android:title="Report">
        <menu>
            <item
                android:id="@+id/report"
                android:icon="@mipmap/ic_launcher"
                android:title="View Report" />
        </menu>
    </item>

    <item android:title="Accounts">
        <menu>
            <item
                android:id="@+id/accounts"
                android:icon="@mipmap/ic_launcher"
                android:title="Manage Accounts" />
        </menu>
    </item>

</menu>